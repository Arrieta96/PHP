<?php


    # Caso "null" 
    // $var;
    // echo $var;

    // "is_null" nos indica si es nulo porque la variable no se ha declarado
    // var_dump(is_null($var));

    // "isset" indica si esta inicializada la variable que tenemos sin declarar
    // var_dump(isset($var));


    # Caso "empty" 
    // "empty" determina si la variable existe y esta declarada, y si el valor 
    // no se evalua como false
    echo $var;
    var_dump(empty($var));




?>